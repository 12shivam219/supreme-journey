<Window x:Class="TrackerAgent.TrayApp.Views.ActivitySummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tracker — My Activity Summary" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#0D1322" Foreground="#F1F5F9"
        FontFamily="Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="My Activity Today" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF"/>
            <TextBlock Text="This transparency dashboard shows you exactly what activity data is recorded on this PC."
                       FontSize="11" Foreground="#94A3B8" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Quick Summary Box -->
        <Border Grid.Row="1" Background="#090D16" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="TOTAL TIME ACTIVE TODAY" FontSize="10" FontWeight="Bold" Foreground="#94A3B8"/>
                    <TextBlock x:Name="TxtTotalTime" Text="0h 00m" FontSize="22" FontWeight="Bold" Foreground="#F59E0B" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="ACTIVE APPS RECORDED" FontSize="10" FontWeight="Bold" Foreground="#94A3B8"/>
                    <TextBlock x:Name="TxtAppCount" Text="0 Apps" FontSize="22" FontWeight="Bold" Foreground="#10B981" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Sessions List -->
        <Border Grid.Row="2" Background="#090D16" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="10" Padding="10">
            <ListView x:Name="LstSessions" Background="Transparent" BorderThickness="0" Foreground="#F1F5F9">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Application" Width="140" DisplayMemberBinding="{Binding AppName}" />
                        <GridViewColumn Header="Window Title" Width="240" DisplayMemberBinding="{Binding WindowTitle}" />
                        <GridViewColumn Header="Duration" Width="90" DisplayMemberBinding="{Binding FormattedDuration}" />
                        <GridViewColumn Header="Time" Width="70" DisplayMemberBinding="{Binding FormattedTime}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Footer -->
        <Button Grid.Row="3" Content="Close" Width="100" Height="32" HorizontalAlignment="Right" Margin="0,16,0,0"
                Background="#1E293B" Foreground="#FFFFFF" BorderThickness="0" Click="BtnClose_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
